<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_aos_vehicle_booking_welcome" model="ir.actions.act_window">
        <field name="name">Welcome</field>
        <field name="res_model">aos.vehicle.booking</field>
        <field name="view_mode">list</field>
        <field name="domain">[("id", "=", 0)]</field>
        <field name="context">
            {"create": False, "edit": False, "delete": False}
        </field>
        <field name="help" type="html">
            <div class="text-center">
                <h1>AOS Vehicle Booking</h1>
                <h2>Demo Version</h2>
                <p>
                    An internal vehicle booking demo for managing vehicles,
                    booking requests, and their workflow.
                </p>
                <h3>Current Capabilities</h3>
                <ul class="list-unstyled">
                    <li>Manage vehicles</li>
                    <li>Create vehicle bookings</li>
                    <li>Process booking workflow</li>
                    <li>View bookings in a calendar</li>
                </ul>
            </div>
        </field>
    </record>

    <record id="view_aos_vehicle_booking_list" model="ir.ui.view">
        <field name="name">aos.vehicle.booking.list</field>
        <field name="model">aos.vehicle.booking</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="requester_id"/>
                <field name="start_datetime"/>
                <field name="end_datetime"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="view_aos_vehicle_booking_form" model="ir.ui.view">
        <field name="name">aos.vehicle.booking.form</field>
        <field name="model">aos.vehicle.booking</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button
                        name="action_submit"
                        string="Submit"
                        type="object"
                        invisible="state != 'draft'"
                    />
                    <button
                        name="action_approve"
                        string="Approve"
                        type="object"
                        invisible="state != 'submitted'"
                    />
                    <button
                        name="action_reject"
                        string="Reject"
                        type="object"
                        invisible="state != 'submitted'"
                    />
                    <button
                        name="action_cancel"
                        string="Cancel"
                        type="object"
                        invisible="state not in ['draft', 'submitted', 'approved']"
                    />
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="vehicle_id" required="1"/>
                            <field name="requester_id" required="1"/>
                        </group>
                        <group>
                            <field name="start_datetime" required="1"/>
                            <field name="end_datetime" required="1"/>
                            <field name="destination" required="1"/>
                        </group>
                    </group>
                    <group>
                        <field name="purpose" required="1"/>
                        <field name="decision_note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_aos_vehicle_booking_calendar" model="ir.ui.view">
        <field name="name">aos.vehicle.booking.calendar</field>
        <field name="model">aos.vehicle.booking</field>
        <field name="arch" type="xml">
            <calendar
                string="Booking Calendar"
                date_start="start_datetime"
                date_stop="end_datetime"
                color="vehicle_id"
            >
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="requester_id"/>
                <field name="state"/>
            </calendar>
        </field>
    </record>

    <record id="action_aos_vehicle_booking" model="ir.actions.act_window">
        <field name="name">Bookings</field>
        <field name="res_model">aos.vehicle.booking</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_aos_vehicle_booking_calendar" model="ir.actions.act_window">
        <field name="name">Booking Calendar</field>
        <field name="res_model">aos.vehicle.booking</field>
        <field name="view_mode">calendar,form</field>
        <field name="view_id" ref="view_aos_vehicle_booking_calendar"/>
    </record>
</odoo>
